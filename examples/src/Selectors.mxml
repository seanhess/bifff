<?xml version="1.0" encoding="utf-8"?>
<mx:Application xmlns:mx="http://www.adobe.com/2006/mxml" layout="absolute" xmlns="http://bifff.seanhess.net/2009">

	<BehaviorMap>
		<Selector match="Button">
			<Listen event="click" call="trace('test')"/>
			<Listen event="creationComplete" call="trace('complete')"/>
			<Bind property="selected" call="asdf.cusot = value;"/>
			<!-- Do you REALLY want this here? -->
			<!-- Or is listen enough :) -->
			<!-- you can always have somebody dispatch an event... -->
			<!-- Maybe more minor changes are in order -->
		</Selector>
	</BehaviorMap>

	<mx:Button id="button" label="test"/>
	<!--<Listen target="{button}" event="click" call="trace('test2')"/>-->
	<!--<Listen target="{button}" event="creationComplete" call="trace('complete')"/>-->

</mx:Application>
